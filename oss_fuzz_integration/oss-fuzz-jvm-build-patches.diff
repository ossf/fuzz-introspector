diff --git a/projects/angus-mail/build.sh b/projects/angus-mail/build.sh
index be6119c3..0f9ecf30 100755
--- a/projects/angus-mail/build.sh
+++ b/projects/angus-mail/build.sh
@@ -55,3 +55,45 @@ LD_LIBRARY_PATH=\"$JVM_LD_LIBRARY_PATH\":\$this_dir \
 \$@" > $OUT/$fuzzer_basename
   chmod u+x $OUT/$fuzzer_basename
 done
+
+# Packing for fuzz-introspector
+jarfile=
+class=
+
+cd $OUT
+for fuzzer in $(find $SRC -name '*Fuzzer.java'); do
+  fuzzer_basename=$(basename -s .java $fuzzer)
+  if ls $fuzzer_basename*.class 1>/dev/null 2>&1
+  then
+    jar cvf $fuzzer_basename.jar $fuzzer_basename*.class
+    if [ -z "$jarfile" ]
+    then
+      jarfile=$OUT/$fuzzer_basename.jar
+    else
+      jarfile=$jarfile:$OUT/$fuzzer_basename.jar
+    fi
+  fi
+  if [ -z "$class" ]
+  then
+    class=$fuzzer_basename
+  else
+    class=$class:$fuzzer_basename
+  fi
+done
+
+# Add jar dependency
+for jar in $ALL_JARS
+do
+  if [[ -f "$OUT/$jar" ]]; then
+    jarfile=$jarfile:$OUT/$jar
+  fi
+done
+
+rm -rf fuzz-introspector
+git clone https://github.com/ossf/fuzz-introspector
+cd fuzz-introspector/frontends/java/soot
+sed -i 's/mvn/$MVN/g' run.sh
+./run.sh -j $jarfile -c $class -m fuzzerTestOneInput
+cd $OUT
+cp fuzz-introspector/frontends/java/soot/*.result ./
+rm -rf fuzz-introspector
diff --git a/projects/antlr4-java/build.sh b/projects/antlr4-java/build.sh
index dacf69d4..e2e6021f 100755
--- a/projects/antlr4-java/build.sh
+++ b/projects/antlr4-java/build.sh
@@ -55,3 +55,45 @@ LD_LIBRARY_PATH=\"$JVM_LD_LIBRARY_PATH\":\$this_dir \
 \$@" > $OUT/$fuzzer_basename
   chmod u+x $OUT/$fuzzer_basename
 done
+
+# Packing for fuzz-introspector
+jarfile=
+class=
+
+cd $OUT
+for fuzzer in $(find $SRC -name '*Fuzzer.java'); do
+  fuzzer_basename=$(basename -s .java $fuzzer)
+  if ls $fuzzer_basename*.class 1>/dev/null 2>&1
+  then
+    jar cvf $fuzzer_basename.jar $fuzzer_basename*.class
+    if [ -z "$jarfile" ]
+    then
+      jarfile=$OUT/$fuzzer_basename.jar
+    else
+      jarfile=$jarfile:$OUT/$fuzzer_basename.jar
+    fi
+  fi
+  if [ -z "$class" ]
+  then
+    class=$fuzzer_basename
+  else
+    class=$class:$fuzzer_basename
+  fi
+done
+
+# Add jar dependency
+for jar in $ALL_JARS
+do
+  if [[ -f "$OUT/$jar" ]]; then
+    jarfile=$jarfile:$OUT/$jar
+  fi
+done
+
+rm -rf fuzz-introspector
+git clone https://github.com/ossf/fuzz-introspector
+cd fuzz-introspector/frontends/java/soot
+sed -i 's/mvn/$MVN/g' run.sh
+./run.sh -j $jarfile -c $class -m fuzzerTestOneInput
+cd $OUT
+cp fuzz-introspector/frontends/java/soot/*.result ./
+rm -rf fuzz-introspector
diff --git a/projects/apache-commons-bcel/build.sh b/projects/apache-commons-bcel/build.sh
index 43e44314..f32eb5bd 100644
--- a/projects/apache-commons-bcel/build.sh
+++ b/projects/apache-commons-bcel/build.sh
@@ -78,3 +78,45 @@ LD_LIBRARY_PATH=\"$JVM_LD_LIBRARY_PATH\":\$this_dir \
 \$@" > $OUT/${fuzzer_basename}
 	chmod u+x $OUT/${fuzzer_basename}
 done
+
+# Packing for fuzz-introspector
+jarfile=
+class=
+
+cd $OUT
+for fuzzer in $(find $SRC -name '*Fuzzer.java'); do
+  fuzzer_basename=$(basename -s .java $fuzzer)
+  if ls $fuzzer_basename*.class 1>/dev/null 2>&1
+  then
+    jar cvf $fuzzer_basename.jar $fuzzer_basename*.class
+    if [ -z "$jarfile" ]
+    then
+      jarfile=$OUT/$fuzzer_basename.jar
+    else
+      jarfile=$jarfile:$OUT/$fuzzer_basename.jar
+    fi
+  fi
+  if [ -z "$class" ]
+  then
+    class=$fuzzer_basename
+  else
+    class=$class:$fuzzer_basename
+  fi
+done
+
+# Add jar dependency
+for jar in $ALL_JARS
+do
+  if [[ -f "$OUT/$jar" ]]; then
+    jarfile=$jarfile:$OUT/$jar
+  fi
+done
+
+rm -rf fuzz-introspector
+git clone https://github.com/ossf/fuzz-introspector
+cd fuzz-introspector/frontends/java/soot
+sed -i 's/mvn/$MVN/g' run.sh
+./run.sh -j $jarfile -c $class -m fuzzerTestOneInput
+cd $OUT
+cp fuzz-introspector/frontends/java/soot/*.result ./
+rm -rf fuzz-introspector
diff --git a/projects/apache-commons-beanutils/build.sh b/projects/apache-commons-beanutils/build.sh
index f62e80d9..6ab5cf71 100644
--- a/projects/apache-commons-beanutils/build.sh
+++ b/projects/apache-commons-beanutils/build.sh
@@ -78,3 +78,45 @@ LD_LIBRARY_PATH=\"$JVM_LD_LIBRARY_PATH\":\$this_dir \
 \$@" > $OUT/${fuzzer_basename}
   chmod u+x $OUT/${fuzzer_basename}
 done
+
+# Packing for fuzz-introspector
+jarfile=
+class=
+
+cd $OUT
+for fuzzer in $(find $SRC -name '*Fuzzer.java'); do
+  fuzzer_basename=$(basename -s .java $fuzzer)
+  if ls $fuzzer_basename*.class 1>/dev/null 2>&1
+  then
+    jar cvf $fuzzer_basename.jar $fuzzer_basename*.class
+    if [ -z "$jarfile" ]
+    then
+      jarfile=$OUT/$fuzzer_basename.jar
+    else
+      jarfile=$jarfile:$OUT/$fuzzer_basename.jar
+    fi
+  fi
+  if [ -z "$class" ]
+  then
+    class=$fuzzer_basename
+  else
+    class=$class:$fuzzer_basename
+  fi
+done
+
+# Add jar dependency
+for jar in $ALL_JARS
+do
+  if [[ -f "$OUT/$jar" ]]; then
+    jarfile=$jarfile:$OUT/$jar
+  fi
+done
+
+rm -rf fuzz-introspector
+git clone https://github.com/ossf/fuzz-introspector
+cd fuzz-introspector/frontends/java/soot
+sed -i 's/mvn/$MVN/g' run.sh
+./run.sh -j $jarfile -c $class -m fuzzerTestOneInput
+cd $OUT
+cp fuzz-introspector/frontends/java/soot/*.result ./
+rm -rf fuzz-introspector
diff --git a/projects/apache-commons-cli/build.sh b/projects/apache-commons-cli/build.sh
index 2fe5fa66..5049db93 100755
--- a/projects/apache-commons-cli/build.sh
+++ b/projects/apache-commons-cli/build.sh
@@ -53,3 +53,45 @@ LD_LIBRARY_PATH=\"$JVM_LD_LIBRARY_PATH\":\$this_dir \
 \$@" > $OUT/$fuzzer_basename
   chmod u+x $OUT/$fuzzer_basename
 done
+
+# Packing for fuzz-introspector
+jarfile=
+class=
+
+cd $OUT
+for fuzzer in $(find $SRC -name '*Fuzzer.java'); do
+  fuzzer_basename=$(basename -s .java $fuzzer)
+  if ls $fuzzer_basename*.class 1>/dev/null 2>&1
+  then
+    jar cvf $fuzzer_basename.jar $fuzzer_basename*.class
+    if [ -z "$jarfile" ]
+    then
+      jarfile=$OUT/$fuzzer_basename.jar
+    else
+      jarfile=$jarfile:$OUT/$fuzzer_basename.jar
+    fi
+  fi
+  if [ -z "$class" ]
+  then
+    class=$fuzzer_basename
+  else
+    class=$class:$fuzzer_basename
+  fi
+done
+
+# Add jar dependency
+for jar in $ALL_JARS
+do
+  if [[ -f "$OUT/$jar" ]]; then
+    jarfile=$jarfile:$OUT/$jar
+  fi
+done
+
+rm -rf fuzz-introspector
+git clone https://github.com/ossf/fuzz-introspector
+cd fuzz-introspector/frontends/java/soot
+sed -i 's/mvn/$MVN/g' run.sh
+./run.sh -j $jarfile -c $class -m fuzzerTestOneInput
+cd $OUT
+cp fuzz-introspector/frontends/java/soot/*.result ./
+rm -rf fuzz-introspector
diff --git a/projects/apache-commons-codec/build.sh b/projects/apache-commons-codec/build.sh
index 1c943daf..8c39d410 100755
--- a/projects/apache-commons-codec/build.sh
+++ b/projects/apache-commons-codec/build.sh
@@ -52,3 +52,45 @@ LD_LIBRARY_PATH=\"$JVM_LD_LIBRARY_PATH\":\$this_dir \
 \$@" > $OUT/$fuzzer_basename
   chmod u+x $OUT/$fuzzer_basename
 done
+
+# Packing for fuzz-introspector
+jarfile=
+class=
+
+cd $OUT
+for fuzzer in $(find $SRC -name '*Fuzzer.java'); do
+  fuzzer_basename=$(basename -s .java $fuzzer)
+  if ls $fuzzer_basename*.class 1>/dev/null 2>&1
+  then
+    jar cvf $fuzzer_basename.jar $fuzzer_basename*.class
+    if [ -z "$jarfile" ]
+    then
+      jarfile=$OUT/$fuzzer_basename.jar
+    else
+      jarfile=$jarfile:$OUT/$fuzzer_basename.jar
+    fi
+  fi
+  if [ -z "$class" ]
+  then
+    class=$fuzzer_basename
+  else
+    class=$class:$fuzzer_basename
+  fi
+done
+
+# Add jar dependency
+for jar in $ALL_JARS
+do
+  if [[ -f "$OUT/$jar" ]]; then
+    jarfile=$jarfile:$OUT/$jar
+  fi
+done
+
+rm -rf fuzz-introspector
+git clone https://github.com/ossf/fuzz-introspector
+cd fuzz-introspector/frontends/java/soot
+sed -i 's/mvn/$MVN/g' run.sh
+./run.sh -j $jarfile -c $class -m fuzzerTestOneInput
+cd $OUT
+cp fuzz-introspector/frontends/java/soot/*.result ./
+rm -rf fuzz-introspector
diff --git a/projects/apache-commons-collections/build.sh b/projects/apache-commons-collections/build.sh
index 9d7239fc..6b6f16af 100644
--- a/projects/apache-commons-collections/build.sh
+++ b/projects/apache-commons-collections/build.sh
@@ -59,3 +59,45 @@ LD_LIBRARY_PATH=\"$JVM_LD_LIBRARY_PATH\":\$this_dir \
 \$@" > $OUT/$fuzzer_basename
   chmod u+x $OUT/$fuzzer_basename
 done
+
+# Packing for fuzz-introspector
+jarfile=
+class=
+
+cd $OUT
+for fuzzer in $(find $SRC -name '*Fuzzer.java'); do
+  fuzzer_basename=$(basename -s .java $fuzzer)
+  if ls $fuzzer_basename*.class 1>/dev/null 2>&1
+  then
+    jar cvf $fuzzer_basename.jar $fuzzer_basename*.class
+    if [ -z "$jarfile" ]
+    then
+      jarfile=$OUT/$fuzzer_basename.jar
+    else
+      jarfile=$jarfile:$OUT/$fuzzer_basename.jar
+    fi
+  fi
+  if [ -z "$class" ]
+  then
+    class=$fuzzer_basename
+  else
+    class=$class:$fuzzer_basename
+  fi
+done
+
+# Add jar dependency
+for jar in $ALL_JARS
+do
+  if [[ -f "$OUT/$jar" ]]; then
+    jarfile=$jarfile:$OUT/$jar
+  fi
+done
+
+rm -rf fuzz-introspector
+git clone https://github.com/ossf/fuzz-introspector
+cd fuzz-introspector/frontends/java/soot
+sed -i 's/mvn/$MVN/g' run.sh
+./run.sh -j $jarfile -c $class -m fuzzerTestOneInput
+cd $OUT
+cp fuzz-introspector/frontends/java/soot/*.result ./
+rm -rf fuzz-introspector
diff --git a/projects/apache-commons-configuration/build.sh b/projects/apache-commons-configuration/build.sh
index 58153f5f..84e8aa12 100755
--- a/projects/apache-commons-configuration/build.sh
+++ b/projects/apache-commons-configuration/build.sh
@@ -60,3 +60,45 @@ LD_LIBRARY_PATH=\"$JVM_LD_LIBRARY_PATH\":\$this_dir \
     # Make the execution wrapper executable
     chmod +x $OUT/$fuzzer_basename
 done
+
+# Packing for fuzz-introspector
+jarfile=
+class=
+
+cd $OUT
+for fuzzer in $(find $SRC -name '*Fuzzer.java'); do
+  fuzzer_basename=$(basename -s .java $fuzzer)
+  if ls $fuzzer_basename*.class 1>/dev/null 2>&1
+  then
+    jar cvf $fuzzer_basename.jar $fuzzer_basename*.class
+    if [ -z "$jarfile" ]
+    then
+      jarfile=$OUT/$fuzzer_basename.jar
+    else
+      jarfile=$jarfile:$OUT/$fuzzer_basename.jar
+    fi
+  fi
+  if [ -z "$class" ]
+  then
+    class=$fuzzer_basename
+  else
+    class=$class:$fuzzer_basename
+  fi
+done
+
+# Add jar dependency
+for jar in $ALL_JARS
+do
+  if [[ -f "$OUT/$jar" ]]; then
+    jarfile=$jarfile:$OUT/$jar
+  fi
+done
+
+rm -rf fuzz-introspector
+git clone https://github.com/ossf/fuzz-introspector
+cd fuzz-introspector/frontends/java/soot
+sed -i 's/mvn/$MVN/g' run.sh
+./run.sh -j $jarfile -c $class -m fuzzerTestOneInput
+cd $OUT
+cp fuzz-introspector/frontends/java/soot/*.result ./
+rm -rf fuzz-introspector
diff --git a/projects/apache-commons-io/build.sh b/projects/apache-commons-io/build.sh
index e60ef093..7a25aaa7 100755
--- a/projects/apache-commons-io/build.sh
+++ b/projects/apache-commons-io/build.sh
@@ -54,3 +54,45 @@ LD_LIBRARY_PATH=\"$JVM_LD_LIBRARY_PATH\":\$this_dir \
 \$@" > $OUT/$fuzzer_basename
   chmod u+x $OUT/$fuzzer_basename
 done
+
+# Packing for fuzz-introspector
+jarfile=
+class=
+
+cd $OUT
+for fuzzer in $(find $SRC -name '*Fuzzer.java'); do
+  fuzzer_basename=$(basename -s .java $fuzzer)
+  if ls $fuzzer_basename*.class 1>/dev/null 2>&1
+  then
+    jar cvf $fuzzer_basename.jar $fuzzer_basename*.class
+    if [ -z "$jarfile" ]
+    then
+      jarfile=$OUT/$fuzzer_basename.jar
+    else
+      jarfile=$jarfile:$OUT/$fuzzer_basename.jar
+    fi
+  fi
+  if [ -z "$class" ]
+  then
+    class=$fuzzer_basename
+  else
+    class=$class:$fuzzer_basename
+  fi
+done
+
+# Add jar dependency
+for jar in $ALL_JARS
+do
+  if [[ -f "$OUT/$jar" ]]; then
+    jarfile=$jarfile:$OUT/$jar
+  fi
+done
+
+rm -rf fuzz-introspector
+git clone https://github.com/ossf/fuzz-introspector
+cd fuzz-introspector/frontends/java/soot
+sed -i 's/mvn/$MVN/g' run.sh
+./run.sh -j $jarfile -c $class -m fuzzerTestOneInput
+cd $OUT
+cp fuzz-introspector/frontends/java/soot/*.result ./
+rm -rf fuzz-introspector
diff --git a/projects/apache-commons-jxpath/build.sh b/projects/apache-commons-jxpath/build.sh
index c33a0488..08bb7a65 100644
--- a/projects/apache-commons-jxpath/build.sh
+++ b/projects/apache-commons-jxpath/build.sh
@@ -55,3 +55,45 @@ LD_LIBRARY_PATH=\"$JVM_LD_LIBRARY_PATH\":\$this_dir \
 \$@" > $OUT/$fuzzer_basename
   chmod u+x $OUT/$fuzzer_basename
 done
+
+# Packing for fuzz-introspector
+jarfile=
+class=
+
+cd $OUT
+for fuzzer in $(find $SRC -name '*Fuzzer.java'); do
+  fuzzer_basename=$(basename -s .java $fuzzer)
+  if ls $fuzzer_basename*.class 1>/dev/null 2>&1
+  then
+    jar cvf $fuzzer_basename.jar $fuzzer_basename*.class
+    if [ -z "$jarfile" ]
+    then
+      jarfile=$OUT/$fuzzer_basename.jar
+    else
+      jarfile=$jarfile:$OUT/$fuzzer_basename.jar
+    fi
+  fi
+  if [ -z "$class" ]
+  then
+    class=$fuzzer_basename
+  else
+    class=$class:$fuzzer_basename
+  fi
+done
+
+# Add jar dependency
+for jar in $ALL_JARS
+do
+  if [[ -f "$OUT/$jar" ]]; then
+    jarfile=$jarfile:$OUT/$jar
+  fi
+done
+
+rm -rf fuzz-introspector
+git clone https://github.com/ossf/fuzz-introspector
+cd fuzz-introspector/frontends/java/soot
+sed -i 's/mvn/$MVN/g' run.sh
+./run.sh -j $jarfile -c $class -m fuzzerTestOneInput
+cd $OUT
+cp fuzz-introspector/frontends/java/soot/*.result ./
+rm -rf fuzz-introspector
diff --git a/projects/apache-commons-lang/build.sh b/projects/apache-commons-lang/build.sh
index 1d79aa98..ac10f346 100755
--- a/projects/apache-commons-lang/build.sh
+++ b/projects/apache-commons-lang/build.sh
@@ -55,3 +55,45 @@ LD_LIBRARY_PATH=\"$JVM_LD_LIBRARY_PATH\":\$this_dir \
 \$@" > $OUT/$fuzzer_basename
   chmod u+x $OUT/$fuzzer_basename
 done
+
+# Packing for fuzz-introspector
+jarfile=
+class=
+
+cd $OUT
+for fuzzer in $(find $SRC -name '*Fuzzer.java'); do
+  fuzzer_basename=$(basename -s .java $fuzzer)
+  if ls $fuzzer_basename*.class 1>/dev/null 2>&1
+  then
+    jar cvf $fuzzer_basename.jar $fuzzer_basename*.class
+    if [ -z "$jarfile" ]
+    then
+      jarfile=$OUT/$fuzzer_basename.jar
+    else
+      jarfile=$jarfile:$OUT/$fuzzer_basename.jar
+    fi
+  fi
+  if [ -z "$class" ]
+  then
+    class=$fuzzer_basename
+  else
+    class=$class:$fuzzer_basename
+  fi
+done
+
+# Add jar dependency
+for jar in $ALL_JARS
+do
+  if [[ -f "$OUT/$jar" ]]; then
+    jarfile=$jarfile:$OUT/$jar
+  fi
+done
+
+rm -rf fuzz-introspector
+git clone https://github.com/ossf/fuzz-introspector
+cd fuzz-introspector/frontends/java/soot
+sed -i 's/mvn/$MVN/g' run.sh
+./run.sh -j $jarfile -c $class -m fuzzerTestOneInput
+cd $OUT
+cp fuzz-introspector/frontends/java/soot/*.result ./
+rm -rf fuzz-introspector
diff --git a/projects/apache-commons-logging/build.sh b/projects/apache-commons-logging/build.sh
index 44eb6216..2fbb4a81 100644
--- a/projects/apache-commons-logging/build.sh
+++ b/projects/apache-commons-logging/build.sh
@@ -67,3 +67,45 @@ LD_LIBRARY_PATH=\"$JVM_LD_LIBRARY_PATH\":\$this_dir \
 \$@" > $OUT/$fuzzer_basename
   chmod u+x $OUT/$fuzzer_basename
 done
+
+# Packing for fuzz-introspector
+jarfile=
+class=
+
+cd $OUT
+for fuzzer in $(find $SRC -name '*Fuzzer.java'); do
+  fuzzer_basename=$(basename -s .java $fuzzer)
+  if ls $fuzzer_basename*.class 1>/dev/null 2>&1
+  then
+    jar cvf $fuzzer_basename.jar $fuzzer_basename*.class
+    if [ -z "$jarfile" ]
+    then
+      jarfile=$OUT/$fuzzer_basename.jar
+    else
+      jarfile=$jarfile:$OUT/$fuzzer_basename.jar
+    fi
+  fi
+  if [ -z "$class" ]
+  then
+    class=$fuzzer_basename
+  else
+    class=$class:$fuzzer_basename
+  fi
+done
+
+# Add jar dependency
+for jar in $ALL_JARS
+do
+  if [[ -f "$OUT/$jar" ]]; then
+    jarfile=$jarfile:$OUT/$jar
+  fi
+done
+
+rm -rf fuzz-introspector
+git clone https://github.com/ossf/fuzz-introspector
+cd fuzz-introspector/frontends/java/soot
+sed -i 's/mvn/$MVN/g' run.sh
+./run.sh -j $jarfile -c $class -m fuzzerTestOneInput
+cd $OUT
+cp fuzz-introspector/frontends/java/soot/*.result ./
+rm -rf fuzz-introspector
diff --git a/projects/apache-commons/build.sh b/projects/apache-commons/build.sh
index cf727898..f39d36ec 100755
--- a/projects/apache-commons/build.sh
+++ b/projects/apache-commons/build.sh
@@ -75,3 +75,45 @@ LD_LIBRARY_PATH=\"$JVM_LD_LIBRARY_PATH\":\$this_dir \
     chmod +x $OUT/$fuzzer_basename
   done
 done
+
+# Packing for fuzz-introspector
+jarfile=
+class=
+
+cd $OUT
+for fuzzer in $(find $SRC -name '*Fuzzer.java'); do
+  fuzzer_basename=$(basename -s .java $fuzzer)
+  if ls $fuzzer_basename*.class 1>/dev/null 2>&1
+  then
+    jar cvf $fuzzer_basename.jar $fuzzer_basename*.class
+    if [ -z "$jarfile" ]
+    then
+      jarfile=$OUT/$fuzzer_basename.jar
+    else
+      jarfile=$jarfile:$OUT/$fuzzer_basename.jar
+    fi
+  fi
+  if [ -z "$class" ]
+  then
+    class=$fuzzer_basename
+  else
+    class=$class:$fuzzer_basename
+  fi
+done
+
+# Add jar dependency
+for jar in $ALL_JARS
+do
+  if [[ -f "$OUT/$jar" ]]; then
+    jarfile=$jarfile:$OUT/$jar
+  fi
+done
+
+rm -rf fuzz-introspector
+git clone https://github.com/ossf/fuzz-introspector
+cd fuzz-introspector/frontends/java/soot
+sed -i 's/mvn/$MVN/g' run.sh
+./run.sh -j $jarfile -c $class -m fuzzerTestOneInput
+cd $OUT
+cp fuzz-introspector/frontends/java/soot/*.result ./
+rm -rf fuzz-introspector
